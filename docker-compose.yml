version: "3.9"

services:
  pneumoai:
    build: .
    container_name: pneumoai
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
    volumes:
      # Persist appointments data across container restarts
      - ./appointments.json:/app/appointments.json
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
